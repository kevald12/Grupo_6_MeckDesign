window.addEventListener("load", ()=>{
    let cartCount = document.getElementById("cart-count")

    // cartCount.innerText(Object.keys(JSON.parse(localStorage.getItem("productAdded"))).length)
    // console.log("el length", Object.keys(JSON.parse(localStorage.getItem("productAdded"))).length)
})